{% extends 'base.html.twig' %}

{% block title %}Pointages{% endblock %}

{% block body %}
    <h1>Pointages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pointage.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ pointage.date ? pointage.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ pointage.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pointages_index') }}">back to list</a>

    <a href="{{ path('app_pointages_edit', {'id': pointage.id}) }}">edit</a>

    {{ include('pointages/_delete_form.html.twig') }}
{% endblock %}
